# Mission Control Agent Configuration Template
# Copy this file to ~/.mission-control/{agent-name}.env and fill in your values

# ============================================
# REQUIRED SETTINGS
# ============================================

# Mission Control Dashboard URL (ngrok or public URL)
MISSION_CONTROL_URL=https://master-controller-tasks.ngrok.dev

# Agent name (must match registration)
AGENT_NAME=your-agent-name

# PostgreSQL connection for agent-to-agent messaging
POSTGRES_URL=postgresql://user:pass@host:port/database?sslmode=require

# ============================================
# AGENT INFO
# ============================================

# Machine hostname (auto-detected if not set)
MACHINE_HOSTNAME=$(hostname -s)

# OpenClaw gateway host for webhook notifications
OPENCLAW_HOST=192.168.1.152:18789

# Webhook URL for wake notifications
WEBHOOK_URL=http://${OPENCLAW_HOST}/hooks/agent

# ============================================
# AGENT CAPABILITIES
# ============================================

# Role description
ROLE="Agent Role"

# Emoji for dashboard display
EMOJI="🤖"

# JSON array of capabilities
CAPABILITIES='["capability1", "capability2"]'

# ============================================
# POLLING SETTINGS
# ============================================

# How often to check for new tasks (seconds)
POLL_INTERVAL=30
